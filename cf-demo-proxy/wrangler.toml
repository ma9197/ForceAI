name = "forceai-demo-proxy"
main = "worker.js"
compatibility_date = "2025-01-01"
# set CLOUDFLARE_ACCOUNT_ID in the env when deploying (kept out of the repo)
workers_dev = false

routes = [
  { pattern = "mirsaidabbasov.com/forceai-demo*", zone_name = "mirsaidabbasov.com" }
]
